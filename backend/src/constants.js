/**
 * Backend-wide constants.
 *
 * This module centralizes reusable literal messages that are shared across
 * services/middleware so user-facing diagnostics remain consistent.
 */
export const CONTRACT_NOT_READY_MESSAGE =
  "Contract client not configured. Set CONTRACT_ADDRESS and ADMIN_PRIVATE_KEY, and ensure RPC_URL is reachable.";
