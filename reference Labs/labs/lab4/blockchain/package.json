{
  "name": "lab4-blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "node": "hardhat node --hostname 0.0.0.0",
    "compile": "hardhat compile",
    "deploy:lab4": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "ethers": "^6.13.2",
    "hardhat": "^2.22.10"
  }
}
